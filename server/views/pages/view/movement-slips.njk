{% extends "partials/layout.njk" %}

{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "partials/movementSlip.njk" import movementSlip %}

{% set pageTitle = "Movement slips" %}

{% block pageScripts %}
  <script type="text/javascript" nonce="{{ cspNonce }}">
    const printOptionsButton = document.getElementById('print-button');
    if (printOptionsButton) {
      printOptionsButton.classList.remove('govuk-!-display-none')
      printOptionsButton.onclick = () => {
        window.print()
      }
    }
    </script>
{% endblock %}

{% block content %}

  {{ govukButton({
      id: "print-button",
      text: "Print movement slips",
      type: "button",
      classes: 'blue-button govuk-!-display-none govuk-!-display-none-print'
  }) }}

  <h1 class="govuk-heading-l govuk-!-display-none-print">{{ pageTitle }}</h1>

  {% if visits.length == 0 %}
    <p class="govuk-body">No visits found for the selected filters.</p>
  {% endif %}

  {% for visit in visits %}
    {{ movementSlip(visit, visit.prisoner, now, not loop.last, 2) }}
  {% endfor %}
{% endblock %}
